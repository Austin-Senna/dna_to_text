__pycache__/
*.py[cod]
*.egg-info/
.venv/
.ipynb_checkpoints/
.DS_Store

# data caches
data/sequences
data/embeddings/
data/embeddings_nt_v2/
data/hgnc/
GenePT_emebdding_v2/
data/dataset.parquet
data/dataset_nt_v2.parquet
data/gene_table.parquet

# E5 TSS parquets — large (1.3 GB total; ~31 MB each). Availability via the Zenodo
# DOI (Austin's deposit), NOT git history. Result JSONs + scripts stay committable;
# the parquets regenerate from build_tss_anchored_datasets.py / the npz cache.
data/dataset_tss_*.parquet

# Phase 4b per-chunk reduction caches — large, fully regeneratable from
# `scripts/run_multi_pool_extract.py --encoder {dnabert2,nt_v2}`
data/chunk_reductions_dnabert2/
data/chunk_reductions_nt_v2/
data/chunk_reductions_gena_lm/
data/chunk_reductions_hyena_dna/
data/tss_chunk_reductions_*/
data/enformer_windows/
data/enformer_features/
# ESM-2 per-gene embedding caches — large, regeneratable from scripts/run_esm2.py
data/esm2_150m_embeddings/
data/esm2_650m_embeddings/

# Pitch deck PDF — kept locally, not authoritative source
Genomics Project (1).pdf
tss_windows_cache.tar.gz
ntv2_tss_*outputs.tar.gz
dist/

# Local snapshot of Austin's data/ tree — large; restore source if caches are lost
data.zip

# Journal-hardening tooling/artifacts — provisioned locally, not committed
tools/
data/annotation/
data/sequences_protein/
data/tss_masked_windows/
data/cluster_work/

# CONFIDENTIAL — this is a PUBLIC repo. Peer reviews, rebuttal strategy, and the
# session handoff stay local: never commit reviews, review analysis, or local paths.
docs/reviews/
MINA.md
STATUS.md

# Runtime logs/outputs (contain local absolute paths)
logs/
outputs/
